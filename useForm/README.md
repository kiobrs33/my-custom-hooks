# UseForm Hook

Ejemplo de Uso

```
    const formInitialState = {
        name:'',
        lastname:'',
        years:0
    };
    
    const [formValues, handleInputChange, handleReset] = useForm(formInitialState);
```
